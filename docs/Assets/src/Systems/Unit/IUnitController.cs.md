# Assets/src/Systems/Unit/IUnitController.cs

_Automatic generated/updated._

```markdown
# Purpose
- Defines the `IUnitController` interface for unit control in the system.

# Public API
- Namespace: `CHAL.Systems`
- Types
  - public interface `IUnitController`
    - Public methods:
      - `EffectReceiver GetEffectReceiver();` (returns an `EffectReceiver` instance)

# Key Behavior & Side Effects
- No explicit state changes or error handling defined in this file.

# Constraints & Failure Modes
- No guards, null/empty handling, or threading/async notes evident.

# Example
- No clear example derivable from the file.

# Unknowns
- No unknowns identified from this file.
```
